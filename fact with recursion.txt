#include<stdio.h>
int fact(int n);
int main()
{
	int n,i;
	printf("enter the number:");
	scanf("%d",&n);
	printf("the factorial of number is %d:",fact(n));
}
int fact(int n)
{
	if(n==1)
	return 1;
	else
	return (n*fact(n-1));
}
